# BlackAgent 优化点执行记录（2026-06-06）

本文件原列 6 项优化要求；本轮已按可自动完成的范围完成代码、数据、验收材料与文档同步。不能由代码伪造的人工复核和真实外部 OCR 对照，明确保留为边界。

## 1. 数据采集与 source skew：已执行增量真实采集

- 代码修正：`src/collector/source_metadata.py` 现在优先使用结构化 `source_type/platform` 判断来源类别，避免仅因 `source_name` 含 `telegram` 就把垂直/众包来源误归为 IM/group。
- 采集脚本增强：`scripts/collect_public_sources.py` 新增 `--max-sources`、可重复 `--source-class`、`--summary-out`，并使用按 `(source_class, source_name)` 轮转的 `balanced_source_slice()`，避免只取展开列表前 N 个来源。
- 新增非 IM 优先配置：`config/intel_sources.collection_optimization.yaml`。
- 已执行授权增量采集：

```powershell
python scripts/collect_public_sources.py --catalog config/intel_sources.collection_optimization.yaml --db data/collection_phase_incremental_rerun.db --fresh --timeout-seconds 30 --max-records 20 --rate-limit-per-minute 6 --retry-attempts 2 --max-sources 8 --source-class social_or_forum --source-class vertical_or_technical --summary-out data/collection_phase_incremental_rerun_summary.json
```

- 证据：
  - `data/collection_phase_incremental_rerun_summary.json`
  - `data/collection_phase_incremental_rerun_stats.json`
  - `data/collection_optimization_report.json`
- 结果：增量重跑 raw=26；`social_or_forum=17`、`vertical_or_technical=9`、`im_or_group=0`。基线 IM/group share 为 `0.9094`，本次增量重跑 IM/group share 为 `0.0`，`improvement.can_claim_raw_skew_improved=true`。
- 边界：本次证明“授权增量重跑的来源结构已显著降低 IM/group 偏斜”；历史 baseline 数据本身未回写重采。

## 2. 人工 held-out 复核闭环：已重跑校验，但不能声称 manual gold

已执行：

```powershell
python scripts/validate_manual_heldout.py --input tests/evaluation/heldout_classification.jsonl --review-csv data/manual_review/heldout_review_task.csv --output data/manual_review/manual_heldout_classification.jsonl --report data/manual_heldout_report.json --min-records 50
```

- 证据：`data/manual_heldout_report.json`
- 结果：`status=insufficient_confirmed_records`，`confirmed_record_count=0`，`min_required_records=50`，`manual_gold_claim.can_claim_manual_gold=false`。
- 结论：复核包和验证门槛已就位，但没有分析员填充 `confirmed/corrected` 行前，不声称 manual gold。

## 3. 真实联网 acceptance E2E：已重跑并达到 high_quality_count >= 2

已执行真实联网 E2E：

```powershell
python scripts/run_agent_cli.py --query "请联网采集公开合规来源中接码、群控脚本、账号交易相关黑灰产线索，启用外部LLM进行必要的意图解析、source query rewrite和线索精炼，输出完整端到端子流程节点、证据链和最终人工复核建议。" --config config/config.real.example.yaml --source-config-path config/intel_sources.acceptance_telegramnav_live.yaml --enable-network --force-real --routing-profile high_recall --max-sources 8 --max-raw-records 160 --max-candidate-clues 40 --max-llm-refine-clues 5 --max-elapsed-seconds 300 --output data/acceptance_real_e2e_run_success.json --show summary
```

- 代码修正：`src/blackagent/interfaces/cli/main.py` 已使用项目根路径解析 fixture/config/corpus/output，修复相对 `--output data/...` 曾写入包目录的问题。
- 证据：
  - `data/acceptance_real_e2e_run_success.json`
  - `data/acceptance_real_e2e_evidence.json`
  - `data/acceptance_real_e2e_evidence.md`
  - `data/acceptance_real_e2e_record_details.json`
- 结果：`status=completed`，`input_count=75`，`selected_source_count=8`，`high_quality_count=2`，目标 `>=2` 已达成；采集覆盖 `im_or_group`、`social_or_forum`、`vertical_or_technical`。
- 证据链边界：
  - `clue_065e2cd8a6a9` 来自贴吧公开搜索，命中“交易所扫货脚本”等脚本/工具交易证据链，仍需人工复核排除教程、经验讨论或误报语境。
  - `clue_5c91a819eb32` 为 Telegram 公开目录共享联系方式重复出现线索，当前主要证明高频共享联系方式，尚未直接证明接码、群控脚本或账号交易服务。
- 结论：E2E 数量目标已完成；“直接证据链”对脚本/工具交易已有较强候选，对所有线索仍不夸大为人工确认事实。

## 4. source skew：已用真实增量证据降低，不只停留在代码策略

- 新增报告脚本：`scripts/build_collection_optimization_report.py`，对比 baseline DB 与增量重跑 DB。
- `scripts/export_collection_phase_stats.py` 已补充 `source_class_counts` 与 `source_skew` 摘要。
- 证据文件 `data/collection_optimization_report.json` 显示：
  - baseline：raw=4163，IM/group share=`0.9094`，forum+vertical=377。
  - rerun：raw=26，IM/group share=`0.0`，forum+vertical=26。
  - `status=rerun_improved_source_balance`。
- 边界：增量重跑 top source share 仍有 `0.3462`，后续若要全量生产级均衡，需更大规模授权采集和持续调度。

## 5. OCR 真实世界验证：本地 hardset 已有强指标，外部 OCR 对照仍是边界

- 证据：`data/eval_ocr_hardset_report.json`
- 当前本地 hardset 指标：
  - entity F1=`0.9677`
  - primary F1=`0.9`
  - secondary F1=`0.7368`
  - hierarchical F1=`0.7`
- OCR adapter 已支持多 engine 对照接口，测试覆盖 `BitmapGlyphOCREngine` 与模拟 cloud callable；`data/ocr_hardset_report.json` 保留 OCR engine comparison 配置。
- 边界：当前环境 `tesseract --version` 不可用，因此不能声称已完成真实截图/Tesseract/云 OCR 对照。下一步需要安装 Tesseract 或接入授权云 OCR，并补真实截图样本，重点继续提升 secondary/hierarchical。

## 6. 文档与答辩验收材料：已同步重建

已执行：

```powershell
python scripts/export_acceptance_e2e_evidence.py
python scripts/build_acceptance_trace_materials.py
```

- 产物：
  - `data/acceptance_real_e2e_evidence.json`
  - `data/acceptance_real_e2e_evidence.md`
  - `data/acceptance_real_e2e_record_details.json`
  - `docs/答辩验收材料/BlackAgent_真实样例逐步明细.md`
  - `docs/答辩验收材料/BlackAgent_验收报告.md`
  - `docs/答辩验收材料/BlackAgent_验收报告.docx`
  - `docs/答辩验收材料/BlackAgent_答辩PPT.pptx`
  - `docs/答辩验收材料/BlackAgent_答辩PPT_可打开修复版.pptx`
- 结果：验收材料已从旧的单线索固定口径改成动态读取当前 E2E 产物；当前输出 `record_count=75`、`high_quality_count=2`、`final_evidence_count=9`、`unique_final_evidence_count=9`，PPT/DOCX zip 检查 `bad_entry=null`。
- 原文附录边界：当前 E2E JSON 未逐条保存 `content_text`；`BlackAgent_原始数据完整内容.md` 是历史同源公开来源复跑原文附录，不能冒充当前 75 条 trace_id 的逐条原文。

## 最终结论

- 已完成：采集来源均衡代码与增量数据证明、真实联网 E2E 目标、source skew 报告、验收证据导出、答辩文档同步、CLI 输出路径修复。
- 明确不声称：人工 manual gold、真实截图/Tesseract/云 OCR 对照、所有候选线索已人工确认属于接码/群控/账号交易。
